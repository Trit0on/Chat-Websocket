{
  "name": "websockets-chat",
  "version": "2.0.0",
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch & npx http-server -p 3000 -o login.html",
    "watch": "tsc --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": "Chat WebSocket avec architecture TypeScript propre",
  "dependencies": {
    "bootstrap": "^5.3.8"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
